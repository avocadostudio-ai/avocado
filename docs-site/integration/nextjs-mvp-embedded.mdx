---
title: "Next.js Embedded MVP"
description: "Default onboarding path for integrating any Next.js site in embedded mode."
---

# Next.js MVP Onboarding (Embedded Mode)

This is the default onboarding path for any Next.js site. For background, see [Core Concepts](/concepts) and the [Integration Overview](/integration).

**Goal**: keep your existing site routes, enable AI editing through Next.js [Draft Mode](/concepts#draft-mode) cookies — no `/preview` route required.

Related:
- [Content Studio Quickstart](/integration/editor-quickstart) — iframe bootstrap URL templates and behavior checks
- [Custom Blocks](/integration/custom-blocks) — register your own component types instead of (or alongside) built-in blocks
- [Architecture](/architecture) — how the three services communicate

## Required contract

1. Add block manifest endpoint:
- `GET /api/editor/blocks`
- returns available component types and prop schemas for safe ops
- generated from adopter-owned component registry (recommended)

The SDK's `createBlocksHandler()` generates this automatically from the block registry.

2. Add Draft Mode entry and exit endpoints (used by the Content Studio):
- `GET /api/draft?secret=...&redirect=/target-path`
- `GET /api/draft/disable?redirect=/target-path`

3. Add secret validation and safe redirect behavior:
- reject missing/invalid secrets
- only allow internal redirects (paths starting with `/`)

4. In page data loading:
- when Draft Mode is disabled, read published data only
- when Draft Mode is enabled, read draft source via `fetchEditorPage()` / `fetchEditorSlugs()` from the SDK

5. Content Studio iframe/bootstrap URL pattern:
- `/api/draft?secret=<DRAFT_MODE_SECRET>&redirect=/<slug>?<context>`

## Block manifest shape (MVP)

Example response:

```json
{
  "version": 1,
  "blocks": [
    {
      "type": "Hero",
      "displayName": "Hero",
      "editablePaths": ["heading", "subheading", "ctaText", "ctaHref", "imageUrl", "imageAlt"],
      "propsSchema": { "type": "object", "properties": { "heading": { "type": "string" } } },
      "defaultProps": { "heading": "New hero heading" }
    }
  ]
}
```

Behavior:
- manifest present: Content Studio enables structural operations (add/remove/reorder/update props)
- manifest missing: degraded mode only (read-only preview or text-only edits)

## How Component Matching Works

The Content Studio does not infer components from DOM class names.
It matches by stable component `type` IDs.

Contract:
1. Manifest entry: `blocks[].type`
2. Content block: `block.type`
3. Site renderer registry key: same `type`

Example:

```json
{
  "blocks": [
    { "type": "Hero", "propsSchema": { "type": "object" } },
    { "type": "FeatureGrid", "propsSchema": { "type": "object" } }
  ]
}
```

```json
{
  "id": "p_home",
  "slug": "/",
  "blocks": [
    { "id": "b1", "type": "Hero", "props": { "heading": "Hello" } },
    { "id": "b2", "type": "FeatureGrid", "props": { "title": "Why us" } }
  ]
}
```

```ts
const rendererRegistry = {
  Hero: HeroSection,
  FeatureGrid: FeatureGridSection
}
```

If a block type exists in content but not in manifest:
- block can still render on site
- Content Studio must not run structural ops for that type (degraded mode for that block/type)

## Environment variables

- Site:
  - `DRAFT_MODE_SECRET`
  - `ORCHESTRATOR_URL` (or your draft backend origin)
- Content Studio:
  - `VITE_SITE_ORIGIN`
  - `VITE_SITE_DRAFT_SECRET` (must match `DRAFT_MODE_SECRET`)

## 30-minute checklist

1. Implement `/api/draft` and `/api/draft/disable`.
2. Implement `/api/editor/blocks`.
3. Verify `/api/draft?secret=wrong` returns `401`.
4. Verify `/api/draft?secret=valid&redirect=/` returns redirect + draft cookie.
5. Verify public route (no draft cookie) does not call draft backend.
6. Point Content Studio iframe to draft entry URL.

## Optional later upgrade

If you want stronger isolation later, add a dedicated `/preview/*` route group.
This is optional and not part of MVP onboarding.
